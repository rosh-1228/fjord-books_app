<% provide(:title, 'All users') %>
<h1><%= User.human_attribute_name(:allusers) %></h1>
 
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>
<%= paginate @users %>
