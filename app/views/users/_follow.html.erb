<%= form_for(current_user.followeds.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit FollowRelationship.human_attribute_name(:follow), class: "btn btn-large btn-primary" %>
<% end %>
