<%= form_for(current_user.followeds.find_by(following_id: @user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit FollowRelationship.human_attribute_name(:unfollow), class: "btn btn-large" %>
<% end %>
